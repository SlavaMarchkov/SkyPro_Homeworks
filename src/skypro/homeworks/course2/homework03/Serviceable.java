package skypro.homeworks.course2.homework03;

public interface Serviceable {
    void updateTyre(Vehicle vehicle);

    void checkEngine(Car car);

    void checkEngine(Truck truck);

    void checkTrailer(Truck truck);
}
